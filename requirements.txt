# Python dependencies for respeaker_ros
# Before pip install:  sudo apt install -y portaudio19-dev
# Then:  pip install -r requirements.txt
# Use numpy>=1.24 so pip uses prebuilt wheels (no build) on Python 3.12+
numpy>=1.24
PyAudio>=0.2.8
SpeechRecognition>=3.8.0
pyusb>=1.0.0
pixel-ring>=0.1.0
# Optional offline STT backend:
vosk
